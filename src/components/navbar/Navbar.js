import React from 'react';
import { Router, Switch, Route } from "react-router-dom";

const Navbar = () => {
    return (
        <div>
            Component!
        </div>
    );
};

export default Navbar;